<div class="row discount-block">
{% for discount in discounts %}
    <div class="col-sm-4 col-xs-6">
        <label for="p-discount-price" class="discount-text">
            {% if discount.next_quantity != false %}
                {{ discount.quantity }} - {{ discount.next_quantity }} {{ text_pieces }}
            {% else %}
                >= {{ discount.quantity }} {{ text_pieces }}
            {% endif %}
        </label>
        <p id="p-discount-price">{{ discount.formatted_price }}</p>
    </div>
{% endfor %}
</div>